name: "Build Docs"
on:
  push:
  workflow_dispatch:
jobs:
  upload:
    runs-on: nix
    steps:
      - uses: actions/checkout@v5
      - name: Build
        run: nix --accept-flake-config build '.#docs-all'
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: docs
          path: |
            ./result/**/*
            ./result/*
