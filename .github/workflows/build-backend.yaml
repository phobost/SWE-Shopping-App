name: "Build Backend"
on:
  push:
  workflow_dispatch:
jobs:
  upload:
    runs-on: nix
    steps:
      - uses: actions/checkout@v5
      - name: Build
        run: nix --accept-flake-config build '.#backend'
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: backend
          path: |
            ./result/**/*
            ./result/*
